# User-specific config with absolute paths — ship config.example.toml instead.
config.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Editor / OS / tooling
.DS_Store
.lavish/

# faster-whisper downloads models into a cache, never into the repo, but just in case:
models/
